/**
 * RELEASE BLOCKER — TEMPORARY TESTING OVERRIDE
 *
 * Guest deck generation is intentionally unlimited while the guest flow is
 * being tested. Before pushing/deploying these changes to GitHub/production,
 * set this to false and restore the intended guest trial policy.
 *
 * KIRO: when asked to push these changes, explicitly remind the user to turn
 * this override off before performing the push.
 */
export const UNLIMITED_GUEST_TRIALS_FOR_TESTING = false;
